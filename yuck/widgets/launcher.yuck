(deflisten distro `bash -c "cat /etc/os-release | grep '^ID' | head -n 1 | cut -d '=' -f2"`)

(defwidget launcher []
    (eventbox :class "launcher-btn"
              :onclick "pkill wofi || wofi --show drun --normal-window --allow-images &"
              :cursor "pointer"
              (label :class "launcher-label-${distro}"
                     :text "")))
