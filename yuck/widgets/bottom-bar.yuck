(include "./yuck/widgets/clock.yuck")

(defwidget bottom-bar []
    (box :class "widget-bottom-bar"
         (left)
         (right)))

(defwidget left []
    (box :halign "start"
         "left"))

(defwidget right []
    (box :halign "end"
         (clock)))
