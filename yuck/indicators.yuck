(include "yuck/network.yuck")
(include "yuck/volume.yuck")

(defwidget indicators []
  (eventbox :class "hover-button"
            :cursor "pointer"
            (box :class "section hover-button-inner"
                 :spacing 12
                 :valign "center"
                 (network)
                 (volume))))
