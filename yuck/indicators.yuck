(include "yuck/network.yuck")
(include "yuck/volume.yuck")

(defwidget indicators []
  (eventbox :cursor "pointer"
            :onclick "scripts/reveal_control_center"
            (box :class "section"
                 (box :spacing 12
                      :valign "center"
                      (network)
                      (volume)))))
