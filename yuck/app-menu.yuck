(include "yuck/system-menu.yuck")

(defwidget app-menu [monitor]
  (box :class "app-menu section"
    (eventbox :onclick "scripts/reveal_system_menu ${monitor}"
              :cursor "pointer"
              :class "app-menu"
              (label :text ""))))
